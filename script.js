document.addEventListener("DOMContentLoaded", function(){

    var date = "JJ/MM/AAAA";
    var PositionPremierSlash = date.indexOf('/');
    var monthPlusYear = date.substring(PositionPremierSlash + 1,10);
    var PositionDeuxiemeSlash = monthPlusYear.indexOf('/');

    function normaliseDate(){
    
   }
})